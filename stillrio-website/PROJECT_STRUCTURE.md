# StillRio — Project Structure Guide

What each folder and file does in plain English.

---

## 📁 Root Level (stillrio-website/)

| File / Folder | What it does |
|---------------|--------------|
| **package.json** | Lists dependencies (React, Next.js, Leaflet, etc.) and scripts (`npm run dev`, `npm run build`). Run `npm install` here to get everything. |
| **package-lock.json** | Locks exact versions of every package. Don't edit by hand. |
| **tsconfig.json** | TypeScript config. The `@/*` path alias points to `src/*`, so `@/components/Hero` = `src/components/Hero`. |
| **next.config.ts** | Next.js config. Add redirects, env vars, or image domains here. |
| **postcss.config.mjs** | Runs PostCSS (and Tailwind) on your CSS. |
| **eslint.config.mjs** | Linting rules. Catches style issues and bugs. |
| **.gitignore** | Tells git to ignore `node_modules`, `.next`, etc. |
| **.env.example** | Template for env vars (API keys). Copy to `.env.local` and fill in. |
| **node_modules/** | All installed packages. Don't edit; regenerated by `npm install`. |
| **.next/** | Build output from Next.js. Don't edit; deleted and recreated on each build. |
| **public/** | Static files. Images and assets you reference as `/filename.svg`. |

---

## 📁 src/ — Your Application Code

All your source code lives here.

---

## 📁 src/app/ — Pages (Next.js App Router)

| File | What it does |
|------|--------------|
| **layout.tsx** | Wraps every page. Sets fonts, metadata (title, description), and the `<html><body>` shell. Every page renders inside `{children}`. |
| **page.tsx** | The home page (`/`). Imports and renders Header, Hero, SocialLinks, VideoEmbed, RoutePlanner. |
| **globals.css** | Global styles: Tailwind import, CSS variables for theme/colors, dark mode. |
| **favicon.ico** | Browser tab icon. |

**Routing rule:**  
- `app/page.tsx` → `/`  
- `app/about/page.tsx` → `/about`  
- `app/blog/[id]/page.tsx` → `/blog/123` (dynamic)

---

## 📁 src/components/ — Reusable UI Pieces

| Component | What it does |
|-----------|--------------|
| **Header.tsx** | Sticky nav bar with "StillRio" logo and links to #social, #planner. |
| **Hero.tsx** | Top section: StillRio branding, tagline, and "Connect" / "Plan Adventure" buttons. |
| **SocialLinks.tsx** | Three cards (YouTube, TikTok, Instagram) with links. Update `href` with your real URLs. |
| **VideoEmbed.tsx** | Embeds a YouTube video. Change `videoId` to your video ID. |
| **RoutePlanner.tsx** | Form for start, end, travel mode. Fetches geocoding + routing, shows distance/duration and map. |
| **MapWithRoute.tsx** | Leaflet map that draws the route polyline. Receives `coordinates` from RoutePlanner. Only loaded client-side (no SSR) because Leaflet needs the browser. |

---

## 📁 public/ — Static Assets

| File | What it does |
|------|--------------|
| **next.svg, vercel.svg, etc.** | Default Next.js starter SVGs. You can replace them with your own logo, favicon, etc. |

Reference them as `/next.svg` or `src="/vercel.svg"` in your code. Put images here for things like logos, open-graph images, etc.

---

## 🔗 How It Fits Together

```
User visits /
    ↓
layout.tsx wraps everything (fonts, metadata)
    ↓
page.tsx renders:
    Header → Hero → SocialLinks → VideoEmbed → RoutePlanner
    ↓
RoutePlanner (on submit) → fetches APIs → sets result
    ↓
MapWithRoute receives coordinates → draws route on map
```

---

## 📁 Folders You Don't Touch

| Folder | Why |
|--------|-----|
| **node_modules/** | Generated by npm. Never edit. |
| **.next/** | Build cache/output. Deleted on `npm run build` or `npm run dev`. |

---

## Quick Reference

- **Add a new page** → Create `src/app/yourpage/page.tsx`
- **Add a new component** → Create `src/components/YourComponent.tsx`, import in `page.tsx` or another component
- **Change global styles** → Edit `src/app/globals.css`
- **Change site title/SEO** → Edit `src/app/layout.tsx` metadata
- **Add static images** → Put in `public/`, use `/filename.png` in your code
